# Copy these values into ~/.config/jev-browser/.env (without quotes).
# Never commit a real key. The browser loop needs no text-generation API key.
TYPESAFE_API_KEY=your_typesafe_api_key
TYPESAFE_MODEL=jev-latest
